# Ilowa Python Backend — copy to .env and fill in real values
# DO NOT commit .env to git (it's in .gitignore)

# ── Supabase (self-hosted on VPS) ────────────────────────────────────────────
# After running vps-setup.sh, grab these from /opt/ilowa/supabase/docker/.env
SUPABASE_DB_URL=postgresql://postgres:YOUR_PG_PASS@localhost:5432/postgres
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# ── Nillion ───────────────────────────────────────────────────────────────────
NILLION_CONFIG_PATH=/opt/ilowa/backend/nillion_config.json

# ── Solana ────────────────────────────────────────────────────────────────────
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet
# program resolver keypair (base58) — used for claiming bet payouts
RESOLVER_KEYPAIR=

# ── AI / external services ────────────────────────────────────────────────────
COHERE_API_KEY=
GLADIA_API_KEY=

# ── auth / security ───────────────────────────────────────────────────────────
JWT_SECRET=
ENCRYPTION_KEY=

# ── server ────────────────────────────────────────────────────────────────────
PYTHON_PORT=8000
LOG_LEVEL=info
